{
  "name": "vernietigingscockpit-code",
  "private": true,
  "workspaces": [
    "apps/cockpit-api",
    "ui"
  ],
  "scripts": {
    "api:dev": "npm run start:dev -w apps/cockpit-api",
    "api:build": "npm run build -w apps/cockpit-api",
    "api:test": "npm run test -w apps/cockpit-api",
    "api:db:migrate": "npm run db:migrate -w apps/cockpit-api",
    "ui:dev": "npm run dev -w ui",
    "ui:build": "npm run build -w ui",
    "ui:lint": "npm run lint -w ui"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.13",
    "tailwindcss": "^4.2.4"
  }
}
